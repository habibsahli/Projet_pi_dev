<form method="post" action="{{ path('app_packpersonnalise_delete', {'id': packpersonnalise.Id_packperso}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ packpersonnalise.Id_packperso) }}">
    <button class="btn">Delete</button>
</form>
