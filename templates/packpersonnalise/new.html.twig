{% extends 'back/base.html.twig' %}

{% block title %}Create new Packpersonnalise{% endblock %}

{% block body %}
    <h1>Create new Packpersonnalise</h1>

    {% for message in app.flashes('error') %}
    <div class="alert alert-danger text-center mb-4" role="alert">
        <strong>{{ message }}</strong>
    </div>
{% endfor %}

    {{ include('packpersonnalise/_form.html.twig') }}

    <a href="{{ path('app_packpersonnalise_index') }}">Back to list</a>
{% endblock %}
